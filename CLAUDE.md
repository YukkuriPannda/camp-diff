# CLAUDE.md

このファイルは、このリポジトリで作業するClaude Code (claude.ai/code) に向けたガイダンスです。

## プロジェクトの状態

MVPは実装済みです。主なコマンドは次の通りです。

| コマンド                   | 内容                                                     |
| -------------------------- | -------------------------------------------------------- |
| `npm run compile`          | 3つのtsconfig（本体・テスト・webview）の型チェックのみ   |
| `npm run lint`             | `src`と`test`にeslintを実行                              |
| `npm test`                 | `out/test/suite/**`をVS Code上のmochaで実行              |
| `npm run test:integration` | 実際のWebRTCピアを立てたエンドツーエンドテスト           |
| `npm run build`            | esbuildで`dist/extension.js`と webview バンドルを生成    |
| `npm run package`          | `.vsix`を生成                                            |

`tsconfig.test.json`の`include`には、`vscode`に依存しない純粋モジュールだけを列挙しています。単体テストしたいロジックは`vscode`非依存のモジュールに切り出し、ここへ追加してください。

## camp-diffとは

camp-diffは、同じGitリポジトリを編集しているチームメンバーが今どのファイル・どの行範囲を編集中かをリアルタイムに共有するVS Code拡張です。マージ・リベース時ではなく編集中の早い段階で、変更の重なり（コンフリクトの可能性）に気づけるようにすることが目的です。詳細な仕様は`README.md`を参照してください。実装時に踏まえておくべき要点は以下の通りです。

- **共有する情報**: ユーザー名、リポジトリ識別情報、ブランチと基準コミット、変更中のファイルパス、変更中の行範囲、最終更新時刻。
- **共有しない情報**: ファイルの本文、追加・削除された行のテキスト（＝diffの中身は一切共有しない）。
- **行範囲の求め方**: カーソル位置ではなく`git diff --unified=0`のハンクから求める（`src/git/diffService.ts`が`git`を実行し、`src/git/diffParser.ts`が行番号だけを抜き出す）。基準は`campDiff.diffBase`で`HEAD`か`merge-base HEAD <remote>/<branch>`を選ぶ。未追跡ファイルはファイル全体を1範囲として扱う。**未保存のバッファはGitから見えないため共有されない。**
- **除外の仕組み**: リポジトリにコミットする`.campdiffignore`（`.gitignore`と同じ書式）で、共有・衝突検知の対象から外すファイルを指定する。
- **UI**: 独自のActivity Barビューから開くサイドバーのツリーに、接続状態・検知したコンフリクト・メンバーごとのファイル/行範囲一覧を表示する。ファイルやメンバーの項目を選ぶと、自分のワークスペース上の該当箇所へ移動する。
- **衝突検知**: 同じファイル内で異なるメンバーの編集範囲が重複・近接している場合に警告する（非ブロッキング＝編集そのものは止めない）。

## MVPのスコープ（README記載）

- VS Code拡張として動作する
- 同じリポジトリ・ブランチで作業するメンバーを表示する
- メンバーごとに変更中のファイルと行範囲を表示する
- `.campdiffignore`に一致するファイルを共有対象から外す
- 同じファイル内で重複または近接する変更範囲を検知する
- 衝突の可能性がある箇所をサイドバーとエディタ上で警告する
- サイドバーから対象ファイルと行へ移動できる
